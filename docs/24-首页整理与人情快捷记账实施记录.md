# 首页整理与人情快捷记账实施记录

日期：2026-09-11。开发和验证均在主目录 `D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project` 完成，保留原有部署相关未提交改动，未提交或推送。

## 用户确认的调整

- 首页保留蓝色账本欢迎卡；账本信息、我的身份、共享与成员移至“我的”。“我的”复用既有资料、成员、邀请入口，保持权限判断。
- 首页移除本月收支区块及对应汇总请求。统计页的本月、全部、自定义范围功能继续保留。
- 首页新增“快捷记账／人情记账”，打开新增账单页，默认支出、人情分类、微信账户和上海时区当天日期；金额、备注和人名留空，点击保存才创建账单。
- 默认分类、账户按启用项目的名称查找；缺少对应项目时留空让用户选择，避免错记成列表首项。再次进入和读取重试保留同一身份的输入，身份切换清空草稿。
- 新增、编辑账单均支持选填人名，详情显示已有的人名。人名独立保存，不拼入备注；去除首尾空格，最多 64 个 Unicode 字符。
- 首页、明细、统计、我的采用文字水平及垂直居中的自定义底栏，四项等宽，当前项绿色；底部安全区和页面内容留白单独处理。全局声明 `usingComponents`，四个页面在 `onShow` 同步选中项。

## 后端与数据库

- 创建、修改、列表、详情、幂等重试均贯通 `personName` / `book_entry.person_name`。
- 独立迁移：`account-book-server/src/main/resources/db/migration/V3__add_entry_person_name.sql`，仅新增可空 `VARCHAR(64)` 字段；原 V1/V2 未修改，历史账单人名为空。
- 旧客户端修改时未提交 `personName` 则保留原值；显式 null 或空白才清空。已补复现测试并修复独立复核发现的数据兼容问题。
- 真实数据库门禁、开发库迁移和运行后端更新本次未执行。新前端保存会携带人名字段，须与新后端及 V3 schema 配套后才能进行保存验收；当前旧后端不支持该字段。

## 验证结果

- 前端全量：29 个文件、332 项测试通过；两套 TypeScript 检查通过。
- 后端离线 `mvnw.cmd -o test`：391 项中 365 通过、26 项真实数据库门禁跳过，失败和错误均为 0。跳过不表示真实 MySQL 已通过。
- 微信开发者工具自带 wcc：20 个页面及自定义底栏组件编译成功。
- 模拟器实际查看：首页新布局、“我的”三组信息、人情快捷表单默认值和人名框均已显示；底栏四项文字居中，首页选中绿色。未在真实账本中提交测试账单，未把视觉检查当作数据库保存通过。
- 独立代码复核发现的旧客户端清空人名问题已修复并复核通过；`git diff --check` 通过。

## 生效及后续验收

依照开发计划中的运行包及真实数据库确认边界，待批准后先执行固定测试库门禁（已扩展 V3 及人名读写用例），通过后备份开发库和当前后端包、应用 V3、受控更新本地后端，再验证新增、编辑、详情与旧客户端兼容。生产部署不在本次范围内。

底栏安全区、键盘和大字号仍需实际设备验证。此次不重启已有后端/SSH、不修改生产库、不清理历史账本数据。

实现参考：[腾讯 CloudBase 纯文字自定义 tabBar 说明](https://docs.cloudbase.net/en/ai/cloudbase-ai-toolkit/prompts/miniprogram-development)、[腾讯小程序平台开发指南](https://main.qcloudimg.com/raw/document/product/pdf/1593_91178_cn.pdf)。
