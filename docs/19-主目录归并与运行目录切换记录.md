# 主目录归并与运行目录切换记录

> 2026-09-08 后续指令：用户要求全部后续开发直接在主目录 `D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project` 进行，不再使用任何隔离项目或新建 worktree。旧隔离目录视为退役归档，不执行物理删除；本次没有读写旧 worktree。下文“保留旧目录”仅表示保留历史备份，不表示继续使用。

日期：2026-09-08。范围：按用户要求将已验收 Task 1–8 交付代码归并回原主目录，并切换本机后端运行位置；不开发 Task 9，不部署生产。

## 结论

- 主开发目录：`D:\Work\Workplaces\privateWork\AAProject\MyTallyBook_Project`。
- 主目录后端已启动，18:59:29 精确进程归属检查通过；Java PID `30436`，JAR 路径为主目录 `account-book-server/target/account-book-server-0.0.1-SNAPSHOT.jar`。
- 原 SSH `44560`、回环 `13306` 保留；后端 `127.0.0.1:7631` 健康 HTTP 200、`UP`。PID 仅为本次证据，不得据此在未来直接终止进程。
- 主目录 `Start-Dev.ps1` 复跑退出码 0，确认 SSH 和后端均复用，无重复启动。
- 微信开发者工具 CLI 打开项目时提示服务端口关闭；未开启该安全设置。**IDE 项目路径切换仍须用户手动导入**，不能把后端健康检查当作小程序页面验收。
- Git 仍保留未提交修改；未创建提交、merge commit、推送或删除分支。这里完成的是文件级代码归并及后端运行切换。

## 文件及配置保护

两个工作区的共同 HEAD 为 `2d260b0d26a7c2f223cd804030533c9229bceca6`。恢复核查时，359 项代码转移清单均已匹配，主目录与源 worktree 的业务代码逐文件一致。源目录仅额外含有四份本次转移辅助文件；没有再次覆盖业务代码。

归并前备份：`backup/main-integration/20260908-172035/`。原主目录 `application.yml` 的配置前缀保留，补充应用必需项；旧首页模板四个文件和旧 `utils/util.ts` 已随既有转移移除，原内容在备份中，可恢复。不要将其恢复到当前页面注册表中以免混用旧模板。

以下配置保留：

| 对象 | 检查结果 |
| --- | --- |
| `secrets/local-dev/keys.dpapi` | 与源目录密文字节一致，现有初始化口令/Pepper 不变 |
| `secrets/local-dev/credentials.dpapi` | 与源目录密文字节一致，现有 AppSecret/开发库密码不变 |
| `account-book-miniapp/project.config.json` | 与源目录一致，AppID 未更改 |
| `account-book-miniapp/project.private.config.json` | 保留主目录私有设置，`urlCheck=false`、基础库 `3.15.1`；未用源目录版本覆盖 |
| V1/V2 SQL | 与源目录及启动器固定摘要一致 |

主目录启动器读取现有配置成功，包含 CurrentUser DPAPI 解密、受限 ACL、AppID 绑定及 Git 忽略规则校验；未显示秘密值、重建或轮换任何密钥。两份 DPAPI、私有项目配置、target、backup 均不进入 Git。

旧 worktree `D:\Work\Workplaces\privateWork\AAProject\MyTallyBook_Project-worktrees\codex-mvp-development` 原样保留。`.superpowers` 中历史门禁/评审材料和旧备份仍在那里；转移清单及 Task 7/8 七组真实 MySQL 成功报告另存主目录归并备份目录，便于追溯。不要从旧目录再启动服务。

## 本次验证

| 验证 | 主目录结果 |
| --- | --- |
| Maven 全量离线测试（18:49:18） | 347 项：321 通过、26 门禁跳过、0 失败/错误；46 份 XML |
| 小程序 Vitest | 21 文件、219 项通过 |
| 两套 TypeScript 类型检查 | 退出码 0 |
| 16 页注册文件完整性 | 四种页面文件全部存在 |
| 微信原生 WXML 编译器 | 16 页编译退出码 0 |
| 运行 JAR 与主目录编译产物 | 134 个 class/资源逐项摘要一致，无缺失 |
| 归并前备份与原配置 | 转移清单中备份摘要全部一致，原 application.yml 配置前缀保留 |
| 主目录 JAR 启动 | 正确 Java/JAR 归属、HTTP 200 / UP |
| 一键启动重复调用 | 退出码 0，复用 SSH/后端 |
| 数据库迁移状态 | 日志确认 V2、两份迁移校验通过、`No migration necessary` |

离线后端报告后缀：`main-cutover-20260908-1848`。真实数据库专项没有在切换时重新执行，仍引用此前独占授权的七组 7/7 成功证据；没有清理开发库、测试库或生产库。

主目录打包 JAR SHA-256：`75DEFAA672AD69BFC4917D59AF158061993F1F1A91913A5923D6B9AE907A2480`。这是主目录构建产物，不要求与旧目录不同时间构建的 ZIP/JAR 字节一致；业务源码、编译测试及内置迁移分别验证。

当前后端通过隐藏的专用 PowerShell 包装进程启动，无需用户密码输入。日志位于：

- `backup/main-integration/20260908-172035/20260908-185206-main-backend.stdout.log`
- `backup/main-integration/20260908-172035/20260908-185206-main-backend.stderr.log`
- `backup/main-integration/20260908-172035/main-backend-launch.json`

## 以后如何启动

PowerShell 任意目录执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "D:\Work\Workplaces\privateWork\AAProject\MyTallyBook_Project\deploy\scripts\Start-Dev.ps1"
```

明确要将主目录新后端源码打包并重启时，在同一命令末尾加 `-UpdateBackend`。日常启动不需要该参数。更新前停止小程序写入操作；不要对正在使用的 JAR 单独执行 Maven clean/package。

微信开发者工具中选择“项目 → 导入项目”，目录选择：

```text
D:\Work\Workplaces\privateWork\AAProject\MyTallyBook_Project\account-book-miniapp
```

使用已有 AppID，编译后继续使用现有开发库；不重新初始化账本，不重新输入初始化口令。新的 IDE 项目目录可能需要重新微信登录，这不代表数据库丢失。若仍打开旧同名项目，先退出该项目，不要删除源目录。

## 后续边界

Task 9 尚未开始；后续开发在主目录进行。旧 worktree 不再作为默认运行源，暂不删除。Git 本地提交及旧分支收尾可另行完成，当前文件级归并不能称为已有 merge commit。真机、跨角色和生产验收仍按原计划单独推进。
