# Task 10 交互、隐私、真机与后续部署准备记录

> 2026-09-11 最新代码状态：此前复核提出的跨会话分类/账户草稿、失败资源重试和 loading/busy 控件交互已修复并独立复核通过；主目录完整小程序回归 332/332、双 TypeScript、20 页原生 WXML 检查通过。此条只关闭代码级收尾，不关闭 Mate 70 Pro/iPhone 15 真机、运行 JAR、真实 MySQL、域名/TLS、Task 11 或 Task 12 门禁。详见[统计范围升级与交互收尾记录](21-统计范围升级与交互收尾记录.md)。

> 2026-09-10 Task 10 子任务 6 最新状态：自动检查已通过，但整体复核未通过，Task 6 尚不能关闭。297 项测试、双 TypeScript 检查、wcc 编译 20/20 页（144590 bytes）、页面四件套 80/80、路由和微信能力审计均通过；9 个受保护路径与本轮基线一致。独立全量复核发现 6 类重要交互缺口，详见下方。本轮仅执行验证和记录，没有修改业务代码、服务、数据库或密钥；未开始 Task 7，也不代表 Task 10 或真机验收完成。

> 2026-09-10 Task 10 子任务 5 最新状态：已在主目录完成隐私文案、静态路由和配置文档审计。`account-book-miniapp/miniprogram/app.json` 当前注册 20 页，四个 tab 为 `首页/明细/统计/我的`；所有注册页均具备 `.ts/.wxml/.wxss/.json`。Focused audit 在文档更新前先完成 67/67 GREEN，补充真实 Page handler 和 `TAB_ROUTES` 断言后复跑为 72/72 GREEN；没有发现需要修改生产路由或隐私行为的缺陷。本子任务不等同于 Task 10 全部完成，也不等同于真机验收完成。

日期：2026-09-10。状态：Task 10 子任务 5 的专项审计已完成；子任务 6 自动检查通过、整体复核待修。下方专项历史结论不覆盖本次全量复核新增发现。未启动服务、清理数据库、部署或上传。

## Task 6 整体复核结果与待修项

独立复核未确认 Critical 问题，但记录了六类 Important：

1. 部分页面的生命周期恢复会清除仍在进行中的提交锁，存在重复请求风险，尤其是邀请创建。
2. 成员列表/详情的旧请求失败回调缺少完整的 generation/identity 隔离，可能覆盖当前状态。
3. 邀请历史读取失败时，错误提示与“暂无数据”会同时显示。
4. 新增账单页缺少分类/账户读取失败后的独立手动重试。
5. 编辑表单首次读取失败后继续输入再重试，存在草稿被返回数据覆盖的路径；已成功加载同一身份资源的 dirty guard 不能覆盖所有失败路径。
6. 计划规定的底部安全区与表单输入尺寸基线尚未落实，实际显示仍需真机验收。

另外，401/退出时已缓存 Tab 数据的清理时序属于待验证的条件风险，不能仅因缺少显式清理回调就宣称发生了数据泄漏。完整位置、影响和复现方向见 [Task 6 全量复核](../.superpowers/sdd/2026-09-09-task10-interaction-privacy-device-implementation/task-6-whole-review.md)，验证命令及产物见 [Task 6 报告](../.superpowers/sdd/2026-09-09-task10-interaction-privacy-device-implementation/task-6-report.md)。

下一步先确认修复上述交互缺口的代码范围，再补对应回归测试、复跑离线/原生验证并复核；不能因当前 297 项测试为 GREEN 就略过这些缺口。Task 7 双设备验收及外部域名/部署条件保持待执行。

## 已确认输入

- 只操作主目录 `D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project`；旧 worktree 仅归档，不使用或删除。
- 用户确认 Task 9 已启动并验证成功。离线和实际操作证据分开；Task 9 固定数据与人工 SQL 对照仍无新执行证据。
- 用户测试设备：华为 Mate 70 Pro、iPhone 15。验收时登记实际系统版本、微信版本、基础库版本和网络；不依据机型把鸿蒙自动记作 Android 验收通过。
- 正式域名备案未完成。服务器公网 IP 为 `117.72.101.42`；用户将在后续提供临时域名，希望预留针对后端 7631 服务的配置入口。
- 2026-09-09 方案梳理开始时曾记录主目录 Git 工作区干净、HEAD 为用户已有 `cbb01f2`；该条是历史基线，不覆盖本次保留的 Task 1–4 未提交实现和 Task 5 测试/文档改动。不提交、推送、重置或创建 worktree。

## 已查明的代码现状

- `account-book-miniapp/miniprogram/app.json` 注册 20 页并声明四个 tab；`tests/config/app-config.test.ts` 对每个注册页检查 `.ts/.wxml/.wxss/.json`，并将 tab metadata 与 `miniprogram/utils/navigation.ts` 的 `TAB_ROUTES` 对照。
- `config/env.ts:3,17-70` 集中配置地址：开发版固定本机 `http://127.0.0.1:7631`；trial/release 地址仍为空且只接受 HTTPS 域名，缺失、IP、localhost、非 443、路径/查询/片段及凭据均在请求前拒绝；不可把 `117.72.101.42:7631` 作为体验版/正式版地址。
- Home 的 `openEntries/openStatistics` 及 EntryDetail 删除后的明细 tab 使用 `navigateToPage`；Settings 的资料、成员、邀请、分类、账户、关于、隐私为栈链接；Profile 成功保存昵称后 `wx.navigateBack`。这些行为均由真实 Page handler 测试覆盖。
- 表单已有防重、原幂等键重试和版本冲突处理，应保留并补齐交互测试，不能改为写请求自动重试。
- 代码实际调用的微信能力包括：`services/wechat.ts:11` 的 `wx.login`；`config/env.ts:69` 的 `wx.getAccountInfoSync`；`services/http.ts:202` 的 `wx.request`；`store/session.ts:130-132` 的 `wx.getStorageSync/wx.setStorageSync/wx.removeStorageSync`；页面使用 `navigateTo/switchTab/redirectTo/reLaunch/navigateBack/showModal/showShareMenu/hideShareMenu/enableAlertBeforeUnload/disableAlertBeforeUnload`；`about/index.ts:15` 和 `invite-create/index.ts:124` 仅在用户点击后调用 `wx.setClipboardData`。源码没有 clipboard read、位置、相册、摄像头、麦克风、手机号或通讯录 API。
- `store/session.ts:12-17,63-73` 的 Storage 只保存 `version/token/expiresAt/environmentId`；用户与账本对象、昵称/头像展示上下文只在内存中由接口响应提供。`profile/index.ts:68-71` 展示既有 `avatarUrl` 并编辑/提交昵称；没有头像选择或媒体上传。
- 隐私页现有文案覆盖微信身份/昵称/头像、会话、共享账本、成员/角色/名额、分类/账户、记账记录、邀请、审计和请求元数据，并明确退出只结束当前设备会话、不删除共享账本或历史；About 备案查询地址和邀请均为显式剪贴板写入，不宣称剪贴板读取。
- 既有 Nginx 模板使用占位 `__API_DOMAIN__`，生产仅 443，反代到服务器 `127.0.0.1:7631`；80 属于既有其他服务，3306/7631 不开放公网。

## Task 10 子任务 5 证据与配置门禁

- 测试证据：focused audit 在文档编辑前 `npm.cmd test -- tests/config/app-config.test.ts tests/navigation-tabs.test.ts tests/config/env.test.ts tests/settings-pages.spec.ts tests/entry-pages.spec.ts tests/privacy-pages.spec.ts` 为 67/67 通过；增加 coverage 后同一命令为 72/72 通过。最终 `npm.cmd test`、`npm.cmd run typecheck`、`git diff --check` 均通过；原生 WXML 源码未改动，不重复编译。详见 [Task 5 子任务报告](../.superpowers/sdd/2026-09-09-task10-interaction-privacy-device-implementation/task-5-report.md)。
- 配置编辑位置：API 地址仅改 `account-book-miniapp/miniprogram/config/env.ts:3` 的 `DEPLOYED_API_ADDRESSES`；公开版本/运营方/联系方式/备案号仅改 `account-book-miniapp/miniprogram/config/app-info.ts:11-18`；页面注册及 tab metadata 仅改 `account-book-miniapp/miniprogram/app.json:2-30`。不在文档或源码写入 AppSecret、数据库密码、初始化口令、Pepper 或真实令牌。
- 仍待配置/核验的外部门禁：临时公网域名、ICP/小程序备案、受信任 TLS 证书、微信 request 合法域名、运营方和联系方式；配置完成前 trial/release 不允许请求。
- 独立状态：代码/离线测试为 GREEN；物理验收未执行。Mate70Pro 的实际 OS、微信版本和基础库证据待登记；若为 HarmonyOS/HarmonyOS NEXT，单独记录，不能计为 Android 覆盖。Task 11 部署材料与 Task 12 体验版/审核/发布仍为后续工作。

## 已确认设计方向（历史批准基线；实现与待验收状态见上方）

1. 推荐原生“首页 / 明细 / 统计 / 我的”四栏；“记一笔”保留明显入口，设置页承接资料、角色、账本、管理入口、退出、关于和隐私。替代方案为维持首页按钮导航，改动较小但不能完成原计划的底部导航体验；不引入第三方 UI 框架或自定义复杂 tabBar。
2. 关于页集中配置版本、运营方、联系方式、小程序备案号和工信部查询地址；未知真实信息保持“待配置/备案中”，不编造，不标记已备案。隐私页按实际代码说明数据、共享范围、会话缓存、剪贴板仅显式写入、退出与数据处理途径；不把退出登录描述为删除共享账单，不增加未经确认的删除账号接口。
3. 统一加载、空状态、网络失败、无权限、邀请失效/满员、编辑冲突的可理解提示；保留表单输入、请求编号、安全重试和已实现的幂等/权限边界；检查键盘、安全区和大字号。
4. 集中分离开发、临时/体验和正式 API base URL；地址不进入业务页面，域名与备案号是公开配置，AppSecret/密码仍留在原加密配置。正式版无有效 HTTPS 域名时阻止启动网络业务，不回退到公网 IP、开发库或临时环境。环境切换不能把原环境会话凭据发送到另一个地址，需要隔离或清除对应会话缓存。
5. 临时域名应作为 HTTPS 公网入口，指向服务器 `117.72.101.42`，通过 Nginx 反代到本机 7631。DNS 记录只决定主机地址，不提供端口转发、TLS 或备案；本次不把后端改成公网 `0.0.0.0:7631`，不修改安全组/UFW/80 服务。
6. 离线类型检查、行为测试、原生 WXML 检查与真实设备人工记录分开。域名/证书/访问链路未准备好时先完成代码和模拟器验证，不宣称双真机已完成。

## 后续顺序与门禁

1. Task 10 设计确认、TDD 实现、独立复核及离线验证；提供用户验收步骤。
2. Task 9 真实统计对照另行取得测试库独占授权后执行，不能复用已消耗的 Task 7/8 清库授权。
3. Task 11 先准备版本化部署、systemd、HTTPS 配置、发布/回滚和备份材料；实际云端部署前核实域名、证书、现有服务、生产库及维护窗口，不混用开发凭据。
4. 待链路就绪在两台真实设备上执行登录、邀请、记账、修改、软删除、统计、权限、退出、错误恢复测试。记录具体 OS；如 Mate 70 Pro 为 HarmonyOS NEXT/原生鸿蒙，单独记录该平台，不自动替代所需 Android 覆盖。
5. Task 12 等小程序备案、域名 ICP、受信任 TLS、微信合法域名、隐私指引及多人测试齐备再推进体验版/审核/正式发布。临时域名不等于这些门禁已通过。

## 外部依据与限制

微信支付官方开发指引的“小程序服务器配置要求”列明小程序请求使用 HTTPS、不能把公网 IP 当作服务器域名、端口须与配置精确匹配以及域名 ICP 要求：[官方开发指引](https://pay.wechatpay.cn/doc/v3/partner/4012088031)。本项目不接入支付，该来源仅用于核对其引用的小程序网络条件。

本轮直接读取微信小程序网络/隐私/配置文档未成功，不能声称已完整复核最新隐私规则。实现相关微信隐私 API 或提交公众平台声明前需再核对官方接口与平台实际指引；本记录的代码使用清单是本地静态检查，不是平台合规审核结论。

## Task 10 子任务 6：离线与原生验证证据（2026-09-10）

### 原生页面编译与页面文件审计

- 工作目录固定为 `D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project`；未使用退休 worktree，未启动 GUI、服务或数据库。
- 使用已校验存在的绝对编译器：
  `D:/Work/Tools/WeChatWebDevelop/微信web开发者工具/code/package.nw/node_modules/wcc-exec/wcc.exe`。
- 输出固定在主目录证据目录：
  `.superpowers/sdd/2026-09-09-task10-interaction-privacy-device-implementation/task-6-wxml.js`。
- 从 `miniprogram/app.json` 读取全部 20 个注册页，逐一传入其绝对 `.wxml` 输入；wcc 退出码为 `0`，输出文件非空，大小为 `144590` bytes。
- 页面四件套审计：20/20 `.ts`、20/20 `.wxml`、20/20 `.wxss`、20/20 `.json`；预期 80 个文件，实际 80 个，缺失 0 个。
- 四个原生 tab 根均具备完整四件套：`pages/home/index`（首页）、`pages/entry-list/index`（明细）、`pages/statistics/index`（统计）、`pages/settings/index`（我的）。

### 路由、tab/stack 语义与能力审计

- `rg -n 'navigateTo|redirectTo|switchTab|reLaunch' account-book-miniapp/miniprogram -g '*.ts'` 命中 51 行；另核对到直接 `wx.*` 原生路由调用 44 行、`navigateToPage` 调用点 5 行。
- `app.json` 的四个 tab 路径与 `utils/navigation.ts` 的 `TAB_ROUTES` 去重集合均为四项且完全一致。tab 根调用统一经过 `navigateToPage`，运行时可用时使用 `wx.switchTab`；其 `navigateTo` 回退仅用于无该能力的测试桩。资料、成员、邀请、分类、账户、关于、隐私以及编辑/详情等保留栈式 `navigateTo`/`redirectTo`；登录、登出及会话恢复使用 `reLaunch` 清理导航栈。
- 源码中的 20 个唯一静态 `/pages/.../index` 目标均属于 `app.json` 注册集合；未发现未注册路由或将编辑页放入 tab 根的配置。
- `rg -n 'wx\.|open-type=|type="nickname"' account-book-miniapp/miniprogram -g '*.ts' -g '*.wxml'` 命中 71 行，唯一微信 API 名称 17 个：`wx.login`、`wx.getAccountInfoSync`、`wx.request`、`wx.getStorageSync`、`wx.setStorageSync`、`wx.removeStorageSync`、`wx.navigateTo`、`wx.switchTab`、`wx.redirectTo`、`wx.reLaunch`、`wx.navigateBack`、`wx.showModal`、`wx.showShareMenu`、`wx.hideShareMenu`、`wx.setClipboardData`、`wx.enableAlertBeforeUnload`、`wx.disableAlertBeforeUnload`。
- 原生标记仅有 `open-type="share"` 1 处与 `type="nickname"` 1 处；静态源码未发现 `wx.getClipboardData`、位置、相册/摄像头、麦克风、手机号或通讯录能力。该项是代码清单，不替代微信平台合规审核。

### 控制器证据与边界

- 控制器于 2026-09-10 19:27 提供：`npm.cmd test` 退出 0（28 个测试文件、297 个测试）；`npm.cmd run typecheck` 的 `tsconfig.json` 与 `tsconfig.test.json` 均退出 0；`git diff --check` 通过。
- 受保护状态控制器已建立 9 个固定路径的基线，其中 8 个路径当前存在；最终比较结果为 9 个固定路径均与基线一致（`application-local.yml` 持续缺失），包括应用/项目配置、`env`/`app-info`、`application.yml`、两份加密 DPAPI 文件和当前后端 JAR。摘要和哈希仅保留在控制器内存，不在本记录输出；该比较不代表长期历史或服务器运行状态。
- 本门禁不改变生产源代码、测试、数据库、服务、凭据或 JAR；不执行真实设备、域名/备案/TLS、微信上传审核、部署或数据库清理。Task 7、Task 10 总体验收、Task 11/12 仍保持待办。

详细命令、原生输出、计数和静态清单见 [Task 6 子任务报告](../.superpowers/sdd/2026-09-09-task10-interaction-privacy-device-implementation/task-6-report.md)。

## Task 10 子任务 6：授权 whole-review 修复波次（2026-09-10）

用户已授权仅在主 checkout 执行一次 Task6 whole-review 的六组 Important 修复；本节记录代码与离线证据，不替代控制器的独立复核，也不启动 Task7。

- 生命周期写锁：settings、profile、home logout、account/category edit/list、entry edit/detail、invite-create 将操作 token 的 settle 与页面 generation 分离。hide/show 只失效旧页面回调，不清除服务端仍在执行的 busy/saving/loggingOut；操作自己的 `finally` 无论页面是否仍 current 都释放自己的锁，旧 success/error/navigation 仍受 generation、session、identity guard 保护。Entry create 保留原有 intent/pending/unknown/completed 与 clientRequestId 语义。
- 成员读恢复：member-list/member-edit 的每轮读取捕获 generation、session revision 和 identity；success/catch/finally/showError 使用同一 current guard。新 show 可在旧 loading 仍 pending 时启动新读取，旧 reject 不能清空新 target、错误、loading 或权限。
- 邀请错误态：邀请历史空态现在排除 `errorMessage`，错误面板、请求编号和显式重试仍保留。
- Entry create 字典读取：增加独立 `canRetryRead` 与 `retryDictionaries`，只重新读取分类/账户，不触发或替换新增账单 intent；成功清除读取错误并保留已填写字段。
- 编辑草稿：account-edit/category-edit 在初始读失败后继续输入，手动读取重试只补充服务端版本/不可变字段并保留本地名称、排序和状态；显式冲突 reload 仍按确认后丢弃草稿。entry-edit 的 read-retry 纳入表单锁定；其初始读失败路径仍由 `canEdit=false` 防止接受输入。
- 交互基线：公共 `.page` 增加 `env(safe-area-inset-bottom)` 底部空间；全局 input 最小高度 `80rpx`、line-height `1.5`，textarea 最小高度 `160rpx` 并保持 multiline 可用。

本波次 focused RED/GREEN、完整测试、双配置 typecheck、原生 wcc 和 `git diff --check` 证据见 [Task6 fix1 report](../.superpowers/sdd/2026-09-09-task10-interaction-privacy-device-implementation/task-6-fix1-report.md)。当前结论是离线实现与测试已 GREEN，独立复核仍待控制器确认；真机安全区/键盘/大字号、401/reLaunch 时序、临时域名/TLS/备案、部署、体验版、审核与发布继续保持 pending。
