# Task 11 生产部署准备记录

日期：2026-09-11  
工作目录：`D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project`

## 本轮结论

已完成 Task 11 第一阶段的仓库材料准备与离线安全检查，未连接服务器、未修改生产、未重启服务、未修改数据库或任何密钥。

已新增或补充：

- `deploy/systemd/account-book.service`：以不可登录的 `accountbook` 用户运行版本化 JAR，使用 `/etc/account-book/account-book.env`，固定 `127.0.0.1:7631`，带受限 systemd 沙箱和失败重启策略。
- `account-book-server/src/main/resources/application-prod.yml`：生产 profile 的回环监听、7631 端口、优雅停机和本机健康端点约束。
- `deploy/scripts/deploy.ps1`：默认 dry-run，计算 SHA-256；只有显式 `-Apply -ConfirmText DEPLOY` 才会执行远端发布。
- `deploy/scripts/install-release.sh`：校验 JAR 摘要、原子切换 `current.jar`、健康检查失败回滚并保留版本。
- `deploy/scripts/backup-mysql.sh`：仅使用 root-owned `0600` MySQL option 文件，压缩、校验、权限、每日 7 份及每周 4 份保留策略均显式处理。
- `deploy/scripts/restore-mysql.md`：先恢复到临时库，再进行生产恢复的维护窗口流程和安全边界。
- `docs/superpowers/plans/2026-09-11-task11-production-deployment-preparation.md`：Task 11 三阶段计划。

## 离线验证

- `deploy/nginx/tests/Test-NginxTemplates.ps1`：通过；确认不占用既有公网 80，HTTPS 仅监听 443，反代目标为 `127.0.0.1:7631`。
- `deploy/scripts/tests/Test-Task11Artifacts.ps1`：通过；确认 systemd 非 root、环境文件、回环端口、发布摘要/确认、原子切换/回滚、备份 option 文件和恢复手册均存在。
- `deploy/scripts/deploy.ps1` PowerShell 语法解析：通过。
- `deploy/systemd/tests/Test-SystemdService.ps1`：通过。
- 后端 `mvnw.cmd -o -q test`：退出码 0；本轮生产配置测试 3/3 通过，真实数据库门禁仍按环境条件跳过。
- 小程序 `npm.cmd test`：28 个测试文件、332 项通过；`npm.cmd run typecheck`：两套配置通过。
- `git diff --check`：通过。
- 独立只读复核发现 `deploy.ps1` 的远端路径注入风险；已为 `-ReleaseRoot` 增加绝对路径字符白名单并复跑通过。当前未发现 Critical/Important 遗留项。
- 当前 Windows 主机未安装 `bash`，因此未在本机执行 shell 的 `bash -n`；Linux 维护窗口前必须使用目标 Ubuntu 的 `bash -n`、`shellcheck`（如可用）和受控 dry-run 再继续。

## 尚未完成的生产门禁

- 服务器只读预检（既有 80/443/7631/3306、Nginx、systemd、UFW、安全组）。
- `accountbook` 用户、`/opt/account-book`、`/etc/account-book/account-book.env` 和日志/备份目录的实际创建与权限核验。
- 真实 API 域名、DNS、ICP/小程序备案、受信任 TLS 证书和微信 request 合法域名。
- 版本化 JAR 上传、systemd 启动、HTTPS/SNI、公网端口边界、重启自动恢复、回滚和临时库恢复演练。

这些动作必须在域名/证书条件具备且用户单独确认维护窗口后执行；不得把 `117.72.101.42:7631` 当作体验版或正式版 API 地址，也不得触碰既有公网 80 服务。
