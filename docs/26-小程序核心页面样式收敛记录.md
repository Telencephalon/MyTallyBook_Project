# 小程序核心页面样式收敛记录

> 2026-09-23 编辑保存返回路径修复：原链路使用 `navigateTo` 从详情进入编辑，再用 `redirectTo` 把编辑页替换为新的详情页，导致每次编辑保存都在原详情上多堆一层详情，返回需重复点击。保存后现优先 `navigateBack` 返回同一账单原有详情，由其 `onShow` 重新加载最新金额、备注和版本；首页最近账单与明细入口的页面实例、列表状态及返回路径均保留，详情再返回一次即可到原入口。连续存在的同一账单旧详情副本在保存返回时一并出栈。仅在直接进入编辑、没有相邻匹配详情时沿用替换当前页的方式，避免误回另一笔账单。
>
> 详情页增加打开编辑的状态锁，阻止连续点击产生多个编辑页；跳转失败可重试，返回详情后解锁。加载、跳转或页面隐藏期间阻止编辑/删除操作，原保存失败、冲突草稿及身份/过期回调保护保留。
>
> 验证：新增 7 项回归，其中 5 项在修复前复现重复详情/重复跳转问题。模拟首页、明细两入口各连续编辑保存 12 次，验证页面栈深度稳定、详情数据刷新及一次返回到原入口；覆盖旧重复详情清理、直接编辑与其他账单详情边界、重复点击和跳转失败重试。全量 459 项测试、双 TypeScript 检查、详情/编辑页 WXML 原生编译与 `git diff --check` 通过。未执行微信真机操作验收，未部署。

> 2026-09-22 统计账户收支与分类占比样式：按用户参考图，为账户汇总卡增加柔和多色渐变、白色描边、圆角和细分隔线；账户名称前增加净额方向图标，正数绿色上箭头、负数红色下箭头、零为中性横线。分类收入/支出卡分别采用浅绿/浅红底色和渐变进度条，增加装饰性的正负号、方向图标，强化标题和金额层级。名称单行省略，大金额允许换行，原金额、百分比及查询口径保留；样式限定在这两个模块，成员排行和每日趋势保持原样。
>
> 验证：统计页面与 Tab 数据页现有 45 项测试通过，统计页 WXML/WXSS 原生编译通过，`git diff --check` 通过。使用实际模板与合成数据，在 390px 检查两账户、收支双卡及 0.66% 小占比；320px 检查长账户/分类名、大金额无横向溢出。未改 TypeScript、后台或接口，尚未执行微信真机视觉验收。

> 2026-09-22 分类与资金账户管理样式：首页最近账单行按 ID 直接进入账单详情，“全部明细”仍进入明细列表；保留加载、退出与身份变化保护。分类列表改为图标、名称、中文收支类型、启停状态与排序组成的紧凑白色卡片；资金账户按微信、支付宝、现金、银行卡和其他账户区分浅色卡片，突出当前余额，负数用红色显示。筛选使用中文胶囊标签，新增按钮统一为蓝色圆角样式，长名称省略显示。
>
> 编辑/启停和删除收进每张卡片右侧“⋮”菜单；同一时间仅展开一个，点击空白、滚动、刷新、离开页面和执行操作时关闭。保留所有者限制、删除二次确认、已引用字典删除限制及账户版本参数；未新增底部资产导航或修改后台余额口径。
>
> 验证：新增 8 项行为回归先失败后通过；全量 452 项测试、双 TypeScript、三页 WXML/WXSS（含共享样式）原生编译和 `git diff --check` 通过。基于真实模板/样式与合成数据的 390px、320px 离线预览确认长名称省略、大金额完整显示、菜单层级及页面无横向溢出。尚未执行微信客户端/真机视觉验收；未部署、提交或推送。

> 2026-09-22 成员排行收支拆分：沿用分类占比方向，成员排行分为绿色收入、红色支出两张卡，各自展示合计、成员金额及占该方向总额的比例，不再合并收支。选择单一收支类型时只查询和显示对应卡；保留创建人、月份/日期范围与 Tab 短时复用。分类和成员共用按方向分组的加载函数与样式。空数据、请求失败、身份变化和过期响应仍遵循原处理。
>
> 验证：新增 3 项成员回归先失败后通过；全量 444 项测试、双 TypeScript、统计页 WXML/WXSS 原生编译通过。320px 离线预览确认成员收支配色、长名称和金额不横向溢出，尚未真机验收。选择“全部”时首次统计加载新增一次成员接口请求，短时切回仍只重新校验身份，未恢复重复业务加载。

> 2026-09-22 统计范围与分类收支拆分：统计范围“本月”改为“月度”，账户收支移动到分类占比上方。分类占比选择“全部”时并行查询收入与支出，分别显示绿色收入卡和红色支出卡；各卡使用对应接口返回的总额、分类金额和百分比，占比基数为该方向的总额，同名分类不会混合。选择单一收支类型时只查询和展示对应卡；保留创建人、月份、全部日期和自定义范围参数，以及失效响应和身份变更保护。任一分类请求失败会清空分组并展示重试错误，空分组显示“暂无收入/支出数据”。后端接口无需修改。
>
> 验证：新增 4 项行为回归先失败后通过，覆盖同分类收支独立、类型切换/空数据、过期响应与部分失败；既有范围/创建人/身份保护测试同步验证两组查询。全量 407 项测试、双 TypeScript 检查、统计页 WXML/WXSS 原生编译、`git diff --check` 通过。基于实际模板和样式的合成数据预览在 390px 确认文案与顺序，在 320px 确认长名称、大金额和滚动布局无横向溢出。尚未执行微信真机视觉验收。

> 2026-09-22 首页欢迎区与内容排版：按用户最新截图恢复原蓝紫渐变欢迎卡片，展示“固定共享账本 / 随手账”、当前账本名称与昵称问候语，保留浅色原生导航。模块顺序调整为快捷记账、收支记账、最近账单、所有者记账字典。最近账单第二行改为人名与备注，分别单行省略超长内容；空字段不显示多余分隔符。“全部明细”右对齐，“本月收支概览”左对齐；提高按钮样式优先级，覆盖微信默认固定宽度与居中边距，并同步修正快捷入口、字典行的宽度。
>
> 验证：首页、导航、人员范围相关 44 项现有测试通过，首页 WXML/WXSS 原生编译及 `git diff --check` 通过。离线预览加入微信默认按钮宽度规则，在 390px 检查模块顺序与对齐，在 320px 检查长账本名、人名、备注及大金额；文字省略生效，无横向溢出。尚未在微信客户端进行视觉验收。

> 2026-09-22 顶部导航回调：按用户最新截图要求，首页恢复应用原生导航默认的浅色背景与黑色标题/胶囊，移除蓝紫色衔接装饰；下方卡片样式保留。首页 WXML/WXSS 原生编译及 JSON 检查通过。

> 2026-09-22 首页按用户 `code_artifact.html` 收敛：改为蓝紫色原生导航及圆弧衔接，移除原欢迎大卡；采用紧凑的双列快捷记账、创建人筛选胶囊和最近账单卡片。收支记账与月度统计组成一张卡片，字典使用彩色图标列表，底部为弱化的“退出当前账号”。保留原导航、数据加载与 OWNER 字典权限。六个图标为本地 PNG，可由 `account-book-miniapp/scripts/generate-home-icons.ps1` 重建，无 CDN/字体运行依赖。
>
> 验证：403 项现有测试、双 TypeScript、22 个 WXML / 25 个 WXSS 原生编译通过。用户附件的 file URL 被浏览器安全策略阻止，未尝试加载其脚本或绕过该地址限制；视觉检查采用项目 WXML/WXSS 与固定假数据生成的独立无脚本预览。390px 和 320px 检查无横向溢出，图标无缺失，长金额缩小后单行完整显示，滚动到底后退出入口在底栏上方可见。未执行微信真机视觉验收，未改后端或部署。

> 2026-09-22 独立筛选交互：取消两行摘要区域共用的展开事件，分类/收支类型/创建人改为独立原生 picker，选择后立即查询并重置分页；分类可单独恢复全部。日期只展开起止日期面板，草稿确认后才查询，取消不改变已应用范围，支持不限日期和起止顺序校验。上方“筛选”仍进入完整面板。新增 5 项回归先复现后修正，全量 403 项测试、双 TypeScript、22 个 WXML / 25 个 WXSS 原生编译通过。

> 2026-09-22 筛选摘要调整：账单明细筛选卡改为两行两列，第一行日期/分类、第二行收支/创建人。统一单元宽度、行距与列分隔，长名称单行省略；保留原展开交互与筛选逻辑。相关 81 项测试和 22 个 WXML / 25 个 WXSS 原生编译通过。

日期：2026-09-22。仅在主目录 `D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project` 修改。

## 已落实

- 按用户确认的首页和后续三张参考图，统一蓝紫渐变、圆角白卡、轻阴影、文字层级和绿色选中导航。新增 10 个本地 PNG 图标及 PowerShell 源生成脚本，总计约 15 KB，无运行时依赖。
- 首页快捷记账双列展示，最近账单使用中文收支标签、人名和日期；所有者专属记账字典权限保留。
- 明细按当前分页结果中的日期分组，保留筛选和翻页；人名、备注单行显示，超长省略。
- 新增、编辑、详情统一金额区和字段行，保留全部事件、错误重试、提交锁定及编辑/删除权限。生活记账默认当前创建人的逻辑未改变。
- “我的”以真实身份和账本信息为主，保留成员、邀请、分类、账户、关于和隐私入口。不新增参考图中的会员等级、虚构 ID、导出备份等功能。
- 统计增加双列收支汇总、结余行、分类占比和原生 Canvas 每日趋势。趋势直接使用当前每日统计分页数据，日期按时间间距排列，收入/支出共用零起点刻度；无效金额留空。保留每日分页及可展开的准确金额明细、账户统计、成员排行和范围查询。排行类型筛选文字明确其适用范围。

## 验证证据

- Vitest：30 个测试文件、356 项测试全部通过，其中新增 6 项趋势坐标计算测试，覆盖空数据、同刻度、零值/单日、日期间距、不修改输入、标签密度和无效金额。
- 两套 TypeScript 检查通过。
- 微信开发者工具自带编译器：22 个 WXML、25 个 WXSS（含公共样式）全部通过。首次样式编译因命令未传入被引用的公共样式而失败；补全编译输入后通过。
- 离线合成数据渲染当前 WXML/WXSS，在 390px 检查主要页面，在 320px 检查 7 页的常规及长内容共 14 个场景，未出现横向溢出。核对长备注省略、大金额、图标、导航留白。趋势组件通过浏览器 Canvas 适配层绘制，无浏览器错误或警告。
- 独立只读代码复核未发现重要回归；`git diff --check` 通过。

## 边界

离线浏览器渲染为布局检查，不能替代微信客户端原生交互验收。开发者工具/真机仍需检查 Canvas、picker、键盘以及安全区。未启动后端、未访问数据库、未部署或上传，也未执行 Git 提交或推送。

离线验证脚本及 14 个页面在当前任务可视化目录：`C:/Users/CR/.codex/visualizations/2026/09/22/01a0c6e2-8d8e-7032-828a-2e7046b417f4/`，入口生成脚本 `render-current.cjs`。示例数据只用于样式检查，不是实际账本数据。

## 后续调整：直接切换收入/支出

根据用户反馈，新增和编辑账单的收支类型改为两个直接点击的按钮，取消类型 picker 弹层。点击后立即切换类型及对应分类，仍通过“保存账单”提交；重复点击已选类型不清空分类。保留加载、提交及重试期间的锁定条件。

新增 4 项行为回归，验证双向切换、其他字段保留、不自动提交、重复点击及无效/锁定状态。相关页面 48 项测试、全量 360 项测试、双 TypeScript 检查及两页 WXML/WXSS 原生编译通过。

## 后续调整：菜单左对齐与切换延迟

- “我的”菜单整行按钮明确占满卡片内容宽度，图标和文字左对齐，箭头靠右；提高选择器优先级以覆盖微信默认按钮固定宽度、居中边距和内边距。同样修正个人资料按钮宽度，避免挤压昵称。
- 原新增页切换类型会刷新身份、分类、账户并锁住表单。现新增/编辑页加载时一次获取全部有效分类，切换时在页面内同步筛选 `INCOME`、`EXPENSE` 及 `BOTH`，只更新必要显示字段，不新增网络请求、不设置 loading。默认人名、快捷分类、原有提交锁定及停用分类兼容保留。
- 分类数据只在当前页面使用，切换前检查页面活跃状态和会话版本。新增页重新显示时刷新数据，编辑页遵循原有草稿加载规则。
- 调整并补充行为测试，验证同步切换、切换零网络请求、通用分类、空分类、离开页面和身份变化保护。全量 364 项测试及双 TypeScript 检查通过；三页模板/样式原生编译通过。离线样式检查加入固定宽度默认按钮规则，确认六个菜单图标统一左边距，个人资料与收支按钮正常显示。尚未执行微信真机验收。

## 后续调整：统计图与底栏重叠

用户截图出现 Canvas 折线和坐标轴覆盖底栏。原底栏使用普通 view，虽有 z-index:100，仍可能受开发者工具的原生 Canvas 层级影响。微信官方 miniprogram-barrage 项目说明开发者工具中的 Canvas 需使用 cover-view 覆盖：https://github.com/wechat-miniprogram/miniprogram-barrage 。

底栏改用 cover-view / cover-image，所有后代保持覆盖组件结构，保留路由、点击绑定、选中图标及安全区留白。图标间距改用 margin，标签设置明确行高；趋势容器增加裁剪并明确画布层级。无需修改统计数据或绘制逻辑。

导航、统计与趋势计算共 33 项测试通过，双 TypeScript 检查和相关 WXML/WXSS 原生编译通过。原生覆盖效果无法由普通浏览器预览验证，仍需在微信开发者工具/真机重新编译后滚动统计页复核。

## 后续调整：从首页进入统计时图表错位

用户复测发现原生 Canvas 仍会在 Tab 切换时脱离卡片，出现在汇总区域。此前 cover-view 只解决导航遮挡，未解决画布定位问题。本次将每日趋势改为普通 view/text 的线段、圆点和坐标轴，移除可见 Canvas，沿用原有金额刻度及日期投影。所有图形元素相对固定高度的卡片内绘图区定位并裁剪。

绘图区在 nextTick 后测量；页面隐藏、组件销毁或新一次测量会使旧回调失效，返回页面和窗口尺寸变化时重新布局。保留两条趋势、图例、每日分页及准确金额明细。

验证：全量 369 项测试通过；新增线段长度/角度与缺失数据断点测试，以及布局后测量、隐藏返回、过期尺寸与销毁回调三项组件生命周期测试。双 TypeScript 检查与组件 WXML/WXSS 原生编译通过。离线 390px 滚动检查和 320px 重排检查中，全部折线点位于绘图区内，无页面横向溢出，浏览器无错误。尚未在微信客户端执行原生入口复测。
