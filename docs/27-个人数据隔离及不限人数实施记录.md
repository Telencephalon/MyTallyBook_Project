# 个人数据隔离及不限人数实施记录

日期：2026-09-22。开发目录：`D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project`。

## 用户确认范围

取消账本的 10 人上限，继续通过有效邀请加入。每个账号的流水以已有 `book_entry.created_by` 隔离，历史账单仍归原创建账号。用户明确选择共用所有者维护的分类、资金账户字典，不创建每人一套字典或复制历史账单。

筛选“创建人”指登录账号，与账单手填“人名”字段不同。OWNER 在首页最近账单、明细和统计页可选择全部成员或某个创建人；三个页面各自保存当前筛选，明细和每日趋势切换时回到第一页。已离开的历史创建人仍可被所有者筛选。

## 权限与接口

| 内容 | OWNER | ADMIN / MEMBER |
| --- | --- | --- |
| 流水列表、数量、详情和最近账单 | 全部或指定创建人 | 仅自己创建 |
| 月/自定义/全部范围的汇总、趋势、分类、账户和成员统计 | 全部或指定创建人 | 仅自己创建；日期范围和分页数量也隔离 |
| 编辑/删除流水 | 可处理全部 | 仅自己创建 |
| 分类和账户字典 | 可读写 | 只读并用于记账选择 |
| 账户余额 | 共享初始余额加全部流水净额 | 初始余额返回 0；当前余额仅个人流水净额 |
| 成员和邀请管理 | 保留原所有者权限 | 保留原成员/管理员权限；不能因此查看他人流水 |

服务端按数据库中的当前成员角色授权，旧令牌中的 OWNER/ADMIN 不扩大权限。非所有者伪造其他 `createdBy` 返回拒绝；访问他人详情返回不存在。所有 SQL 创建人条件使用参数绑定。幂等请求只对原创建人返回记录，不向其他账号透露记录的删除状态。

- `GET /api/v1/entries` 保留 `createdBy`；列表与总数强制同一范围。
- 五个统计 GET 接口新增可选正整数 `createdBy`；OWNER 不传表示全部，非 OWNER 不传表示本人，非 OWNER 传其他人会被拒绝。
- 账本和成员列表响应的 `maxMembers` 为 `null`，表示人数不限。新小程序同时兼容历史正整数响应与新空值合同，页面按新规则展示不限人数。
- 邀请仍验证到期时间、单次使用、成员/用户状态与唯一所有者，保留原事务锁。初始化仍受一次性口令保护，没有开放注册入口。

## 历史数据与发布兼容

本轮不新增数据库迁移，不修改历史 Flyway 文件。`max_users`、`max_members` 保留为历史元数据，不再决定接口返回或加入资格。数据库已有 `(ledger_id, created_by, entry_date)` 索引继续供个人流水查询使用。

上线时须同时更新后端和小程序，建议先更新后端权限再发布小程序。只更新小程序不能实现数据隔离；当前运行 JAR 与正式环境均未在本轮更新。未连接开发库、测试库或生产库，没有清库、改密钥、初始化、提交或推送。

## 验证记录

- 初始回归复现第 11 位成员被拒、越权读取和管理员跨账号操作；实现后对应测试通过。
- 完整后端离线回归：455 项中 429 通过、26 个真实数据库入口按门禁跳过，0 失败/错误。执行前在子进程清空 `DB_TEST_URL`。
- 小程序最终回归：32 文件、398 项测试通过，双 TypeScript 检查通过，22 个 WXML 和 25 个 WXSS 原生编译通过。
- 后端审查修正：幂等重试先校验创建者，再检查删除状态。新增三角色的已删除/未删除回归，先复现 3 项失败，再修正；账单服务和 HTTP 共 35 项相关回归通过。
- 前端审查修正：分类/账户页首次加载失败时允许只读重试；账单保存中发生降权时清空旧表单并丢弃旧响应。先复现失败，再修正，两名独立审查者确认关闭；另独立运行 6 个恢复/降权场景通过。
- 两份独立审查结论均无剩余发现；`git diff --check` 通过，数据库迁移目录无改动。
- 真实 MySQL 多账号对照、第 11/12 位并发邀请和微信客户端交互验收尚未执行，离线结果不代表这些已通过。

具体拆分计划见 [personal-data-scope](superpowers/plans/2026-09-22-personal-data-scope.md)。

## 2026-09-23：手机正式版仍可跨创建人查看的排查

用户确认问题出现在手机正式版：最近账单、明细和统计仍显示全部成员流水，只有编辑、删除受限。

### 已核实的证据

- 当前源码已在 `EntryService` 的列表、数量、详情及 `StatisticsService` 的全部统计入口应用 `DataScope`。非 OWNER 不传 `createdBy` 时也强制使用数据库中当前登录成员的 userId；ADMIN 与 MEMBER 使用同一读取限制。SQL 查询和数量、日期范围均绑定此创建人。
- 本机旧 `target/account-book-server-0.0.1-SNAPSHOT.jar` 最后修改时间为 2026-09-12，缺少 `DataScope.class`。
- 保留的首发上传包 `server-upload-20260920-131055/account-book-server-20260920-131055.jar` 同样缺少该类，SHA-256 为 `a9a45841433e922c147a6a45a5b31ad05ce324cf3b35fb57313309d3b00866e9`，与正式首发记录完全一致。隔离实现于 2026-09-22 才加入。
- 用户随后提供正式 SSH 终端截图，确认 `current.jar` 指向 `/opt/account-book/releases/account-book-server-20260920-131055.jar`，摘要与上述首发包完全一致，服务状态为 `active`。由此确认正式后端尚未更新隔离实现，不能通过仅更新小程序界面补齐读取权限。

### 本地验证与待发布包

- 本轮未改动业务权限源码；先运行隔离相关 83 项测试，全部通过，包括非所有者默认查询、旧 OWNER 令牌与实时角色不一致、伪造创建人、列表总数、统计及日期范围的隔离。
- 主目录执行离线 `mvn -o package`，在构建子进程清空真实数据库测试入口。457 项中 431 通过、26 跳过，0 失败/错误；构建成功。
- 构建前确认没有正在使用主目录 JAR 的 Java 进程，旧包保留在 `backup/pre-isolation-build-20260923-195041/`。
- 新发布包目录：`account-book-server/target/server-upload-20260923-195353/`。包含版本化 JAR、安装器、备份脚本、`SHA256SUMS` 及 `release-evidence.json`，不包含生产环境文件或 service 替换文件。
- 新 JAR：`account-book-server-20260923-195353.jar`，SHA-256：`a9b1a56c6d98d9ee84bfa8b6e736ea7637d2a3691ab5ae1b8a6583979eca6395`。已检查包含隔离类，V1～V4 迁移文件与首发 JAR 逐文件摘要一致。

### 当前线上状态与后续步骤

尚未部署，也未进行真实多账号验收。本机 SSH 非交互认证失败；用户在独立终端连接服务器，该终端不属于当前任务可控制的终端。用户通过截图提供的只读结果已确认首发包仍在运行。

本次版本的具体上传、预检、备份、安装及健康验证命令见 [本次隔离更新步骤](28-正式环境个人数据隔离更新步骤.md)，按 [正式部署手册第 11.2 节](26-备案完成后首次服务器迁移与小程序发布手册.md#112-服务器后端升级) 编排。正式库仍以现场核实为准，现有记录为 `account_book_dev`。该手册已指出旧 `deploy.ps1 -Apply` 的备份库名和健康检查问题，本次未执行该入口。

上线验收需要所有者及两个不同的非所有者账号：最近账单、明细列表/总数、全部统计应分别符合全部或本人范围；他人详情不可读取；所有者可按指定创建人筛选。用户手填的“人名”不决定数据归属。正式环境更新及手机复验完成前，不能将本记录解释为线上已修复。
