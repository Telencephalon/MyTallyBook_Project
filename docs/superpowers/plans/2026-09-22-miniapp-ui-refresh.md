# 小程序界面优化实施计划

> 执行方式：使用 superpowers:executing-plans 在主目录逐项实施，完成后按 requesting-code-review 做一次独立复核。遵循 AGENTS.md，不创建 worktree，不提交或推送。

**Goal:** 首页采用用户 2026-09-22 确认的截图布局，其余核心页面采用已确认的静态草图样式。

**Architecture:** 保留原生小程序 Page、接口、会话及事件处理，只调整展示结构、样式与本地导航图标。共用样式通过页面级导入复用，避免改变未涉及页面的布局。

**Tech Stack:** WXML、WXSS、TypeScript、原生 image、本地 PNG 图标、Vitest、微信 wcc/wcsc。

**Spec:** 当前会话确认的首页参考图与上一轮五页静态草图。最新首页截图优先于参考 skill 的原子卡片 4px 圆角规则；完整页面不采用 AI 宿主的固定卡片比例约束。

## Global Constraints

- 工作目录仅 `D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project`。
- 不连接数据库、不启动或更新后端、不部署。
- 首页：蓝紫渐变欢迎卡；图标加说明的双列快捷入口；最近账单日期、分类、中文“收入/支出”标签、人名、金额；一行收支记账操作；图标底栏。
- 保留首页所有者字典可见性及其他页面原有权限；保留现有全部明细、退出入口的可达性。
- 保留生活记账默认创建人、人情记账空人名、备注省略及所有保存/重试/冲突/禁用语义。
- 用户随后补充明细、我的、统计参考图：核心页面统一渐变页头、圆角白卡、轻阴影和图标导航，替代上一版 4px 卡片圆角；正文仍采用 17/15/12px 层级。
- 仅使用真实已有字段，不新增会员等级、外部账号 ID、账本创建时间、导出备份或个性化设置等未实现功能。
- 明细按当前页数据的日期分段，不改变查询范围、排序或分页；统计新增当前页每日数据折线图，保留精确每日数据与分页。

## Review Focus

- 窄屏与长文本：日期/分类/人名省略或合理换行，金额不挤出页面。
- 未加载、错误、空列表、普通成员及管理员：不能丢失恢复按钮或显示所有者入口。
- 表单提交中、不确定重试、编辑冲突、已停用字典：原有禁用条件和提示保持有效。
- 图标底栏：四项路由、选中态、可访问名称和底部安全区正确。
- 多页样式复用：不污染登录/初始化/成员/邀请等未改动页面。

## Tasks

### 1. 首页与底栏

- [x] 新增本地图标及源生成脚本；不增加运行时依赖。
- [x] 重排首页 WXML、覆盖对应 WXSS；保持现有事件绑定。
- [x] 为底栏数据补充正常/选中图标路径，更新布局及全局 tab 页底部留白。
- [x] 运行既有首页、底栏、导航和会话回归；原生模板/样式编译。

### 2. 其余核心页面

- [x] 新增页面级共用样式，统一明细分组、表单行、提示和主副按钮。
- [x] 新增与编辑表单共用金额区、字段区、备注区样式；类型 picker 标签中文化，保留索引处理。
- [x] 更新详情展示；保留全部内容与编辑/删除权限。
- [x] 统计汇总改为收入/支出双列加结余行；保留全部查询、分页、排行和错误分支。
- [x] 我的页优先呈现身份，其次账本信息及原有功能入口。
- [x] 运行小程序全量测试和双 TypeScript 检查，不新增只匹配源码文本的样式测试。

### 3. 验证与交付

- [x] 所有页面 WXML/WXSS 原生编译，检查引用图标存在。
- [x] 用合成数据渲染修改后的模板/样式，检查 320px/390px 及长内容；不对真实账本执行操作。
- [x] 独立复核差异，处理影响使用的发现。
- [x] 保存实施结果；明确离线/预览证据与微信真机验收的区别。
