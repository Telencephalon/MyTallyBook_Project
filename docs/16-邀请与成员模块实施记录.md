# 邀请与成员模块实施记录

**日期：** 2026-09-05～2026-09-06  
**状态：** 本地后端已更新并重启，健康 UP；用户随后回复“都以正常”，确认当前运行及页面恢复正常。已保存进度并暂停后续开发。Task 6 非容量真实 MySQL 验证 16/16 通过；容量及跨角色/跨设备完整验收仍未完成，Task 7/8 未开始，不等同于整体发布验收完成。

## 2026-09-06 一键更新与实际重启完成

用户明确批准默认启动/复用和 `-UpdateBackend` 备份、打包、后端重启模式，随后要求以最快方式直接停止并重启。仅恢复本次更新范围，不开始 Task 7/8。

- 启动器修复管理员进程查询、精确创建时间、原生句柄退出等待及跨模块校验问题；独立复核无 Critical/Important 项。实际服务停止前，主代理 Windows PowerShell 5.1 专项 41/41 及真实只读预检通过。PowerShell 7 的 DPAPI 预检曾失败，未停止服务；实际入口始终使用用户命令中的 Windows PowerShell 5.1，预检成功后才执行更新。
- 已通过 `Start-Dev.ps1 -UpdateBackend` 更新，入口退出码 0；原后端 42896 已退出，新 Java 31920 监听回环 7631，健康 HTTP 200、UP。原 SSH 18576（回环 13306）保持不变。
- 新 JAR 为 61,013,125 字节，SHA-256 `3F8B913C27F06B4A5D64349E91C2F0B74609F450B18C6CF470B63E5C0DE05F72`。邀请/成员控制器均存在且与本次编译 class 摘要一致；打包中仅有原 V1/V2。
- 新的旧包备份为 `backup/local-backend/account-book-server-20260906-082328-2724801-865cecb7fe86.jar`，60,951,868 字节，SHA-256 与原包 `865CECB7FE86DA2901FD7264EA7D439232982CC88A041E91DEAF45FAF678F5BE` 一致。此前备份也保留。
- 本次离线 package 报告为 32 份、289 项，270 通过、19 个真实数据库入口跳过、0 失败/错误。没有重跑真实数据库门禁或清库；16 项真实报告独立保留，不混算为本次打包结果。
- 两份 DPAPI 文件密文摘要、大小及修改时间不变；不改密码、AppID、初始化口令、Pepper 或数据库。普通 `Start-Dev.ps1` 再次实际运行返回退出码 0，SSH 和新后端均复用，未再次重启。

用户随后回复“都以正常”，本次更新后的基础运行/页面恢复已获确认，无需重复同一项验证。不由此自动勾选邀请消费、角色调整、容量竞争或跨设备等完整场景。以下 15:02 及以前的“尚未更新”文字为历史证据，以本节状态为准。

## 2026-09-06 后续开发顺序与收尾预检

用户最新要求：先做 Task 6 必要收尾，再逐项实现 Task 7「分类与资金账户」、Task 8「收支记账」。此前暂缓“争夺第 10 个名额、拒绝第 11 人”验证的指令继续有效；不删除相关业务规则或将其标记为通过。

**最新指令覆盖后续执行安排：Task 6 本次已授权收尾结束后保存进度并暂停，不开始 Task 7/8。** 本次 16 项验证已通过；后端自动更新在停止旧进程时未能完成，因此保留旧应用，保存这一未完成交接点并暂停，不将 Task 6 全部验收标记完成。Task 7 仅已确认删除行为，尚未实施。

- 13:36:27 后端离线全量复跑：289 项、0 失败、0 错误、19 跳过，即 270 项通过；四个 `DB_TEST_*` 输入均在测试进程中清空，未访问实际数据库。原有 Mockito 动态 agent/CDS 警告仍存在。
- 13:35:55 小程序离线复跑：16 文件、160 项通过，两套 TypeScript 类型检查通过。
- 端口记录更正：此前命令输出未完整返回，不能据此判定未监听。本次通过 .NET/netstat 完整复核，SSH PID 13232 监听 `127.0.0.1:13306`、原后端 PID 50904 监听 `127.0.0.1:7631`。限定信息查询核对了可执行文件及命令参数，分别通过原隧道/本项目 JAR 归属校验；健康检查为 UP。复用现有运行窗口，本次未启动或停止它们。PID 仅供本次记录，不能用于未来未经复核的停止操作。
- 独立只读核查确认，容量竞争与第 11 人拒绝都在 `group1_lastSeatAndEleventhRejection`。下一次受控验证只选择 `group2` 至 `group9` 的八个明确方法，RC/RR 展开后应为 **16 项、0 失败、0 错误、0 跳过**。原完整 18 项门禁仍不能标记通过。
- 旧操作脚本的 `surefire.reportsDirectory` 参数没有在当前 Surefire 3.5.6/POM 中绑定；旧脚本还硬编码整个测试类及 18 项。因此**不要直接重跑旧脚本**。拟最小修正为正向方法清单及唯一 `surefire.reportNameSuffix`，在默认报告目录读取此次独有 XML，不修改 POM 或生产代码。
- 主代理 13:42:41 使用空数据库变量执行了真实 Maven 选择器/报告路径离线检查：八个指定方法全部按门控跳过，group1 未被选择；唯一后缀 XML 确实生成。这只证明选择器和报告路径有效，不证明数据库场景通过。报告为 `account-book-server/target/surefire-reports/TEST-com.mytallybook.accountbook.database.InviteMemberMysqlIntegrationTests-task6-selector-offline-26380326f4d84204a1f2f6f0ede31dcd.xml`；不可将其与全量回归报告混算或作为真实门禁结果。

用户最新“确认允许”已批准：修正操作脚本并离线验证后，在独占测试窗口中仅清理固定 `account_book_test`，使用专用测试账号进行上述 16 项真实验证；只有全部通过后才备份原 JAR、更新并启动本项目本地后端，完成邀请/成员基础联调。开发库和生产库不清理、不重新初始化，不改 V1/V2、密码、AppID、初始化口令或 Pepper；失败不自动重试或额外清理。

操作入口修正现已完成并通过独立复核；补齐 PowerShell 空白/小数计数强制转换缺口后，主代理独立运行 Windows PowerShell 5.1 离线测试，20 项通过、0 失败。明确八方法选择器、独有报告后缀及严格 16/0/0/0 计数校验，未更改测试用例、生产代码或 POM。

14:32:30 打开独立测试窗口（外层 PowerShell 51484、内层 54260）；用户完成 TEST 和隐藏密码输入并回复“已输入”。本次 Maven 40912、Surefire 1032 使用批准的八方法清单、单 fork。14:55:40 生成完整报告，持有进程句柄的观察器取得 Maven 退出码 **0**；14:57:26 主代理独立严格解析报告，确认 **16 项、0 失败、0 错误、0 跳过**，group2～group9 每组两项，测试进程均已退出。

- 原始报告唯一后缀：`0fa1fce98679417caa068967e6d99a4f`。已另存至 `.superpowers/sdd/2026-09-05-task6-invites-members-implementation/mysql-gate-approved-16-20260906-145540/`，源与留存 XML 的 SHA-256 均为 `383E27E908CA6F374F73B63E6CB6A7E7E37ADCE219334BD873916194CE89B35B`。留存报告受 Git 排除，不在聊天或公开文档中展开环境属性。
- 14:58:50 原 JAR 已备份至 `backup/local-backend/before-task6-20260906-145850-ee11821d/account-book-server-0.0.1-SNAPSHOT.jar`，60,951,868 字节；源/备份 SHA-256 均为 `865CECB7FE86DA2901FD7264EA7D439232982CC88A041E91DEAF45FAF678F5BE`。
- 15:00:20 再次核对 PID 50904 的创建时间、Java 路径和本项目 JAR 参数后，普通 `Stop-Process` 未能完成（`ProcessCommandException`），旧后端仍在管理员 PowerShell 窗口运行。只读权限探针能够取得 TERMINATE 访问句柄，因此不能将该失败简单写成“系统拒绝终止权限”；没有进一步强制终止或提权。现有启动器的 CIM 路径/参数查询也无法完整读取管理员 SSH 进程，自动停启链路未完成。
- 15:02:22 原后端健康仍 UP，原 SSH 13232 保留；原 JAR、两份 DPAPI 配置的大小和修改时间均未变。**未执行 package、未替换 JAR、未更改密码或密钥。** 测试库按授权清理了可丢弃的测试对象/数据，数据库本身保留；开发库、生产库未清理。

下次继续从本地后端更新开始：在原管理员后端窗口停止旧应用（SSH 窗口保持打开），核对端口释放后再受控打包；在能正确识别原 SSH 的管理员上下文中启动并复用现有加密配置，完成健康和邀请/成员基础联调。若选择修改停启脚本兼容性，先单独确认最小修正设计，不跳过进程归属校验。当前不自动继续这些操作。

Task 7、Task 8 尚未开始功能代码实现。Task 7 按既有权限、唯一性、停用历史引用保护、创建时初始余额和计算当前余额推进；之后 Task 8 实现新增/详情/列表/筛选/修改/软删除、UUID 幂等、版本冲突和创建人权限。新增业务模块的详细合同在进入对应阶段前依照既有设计核对，不提前引入新迁移或把统计 Task 9 纳入本轮。

## 1. 已确认范围

用户已同意[详细规格](superpowers/specs/2026-09-05-task6-invites-members-design.md)，包括管理者失权后同步撤销未使用、未过期邀请；退出登录与会话恢复已由用户确认正常。之前登录、初始化、首页、身份入口及昵称修改的验收保持有效。

开发按[实施计划](superpowers/plans/2026-09-05-task6-invites-members-implementation.md)推进。工作仅在原 `codex/mvp-development` 隔离目录内，不提交或推送、不覆盖既有改动、不改 V1/V2 或运行 JAR。2026-09-06 用户报告电脑重启并明确要求恢复 SSH 和应用：仅恢复原隧道及原后端版本，由已有脚本加载本机加密配置；不输出或改写敏感值，不把恢复启动视为部署新功能。

## 2. 实施进度

| 单元 | 状态 | 验证边界 |
|---|---|---|
| 统一写事务锁、登录/资料最新身份校验 | 代码及离线审查通过 | 修正配置缺失错误码后定向 27 项通过；完整后端 139 项中 129 项通过、10 项数据库门禁跳过 |
| 邀请创建、列表、撤销、接受 | 代码及离线审查通过 | 定向 94 项通过；完整后端 204 项中 194 项通过、10 项数据库门禁跳过；格式整理后定向 27 项通过，真实并发尚未验证 |
| 成员列表、角色、退出/移除、转让 | 代码及离线审查通过 | 定向 68 项通过；完整后端 266 项中 256 项通过、10 项数据库门禁跳过 |
| 小程序四个页面及入口 | 分项审查与残留问题复审通过 | 最新 160 项/16 文件通过、两套 TypeScript 检查通过；不等同于微信渲染验收 |
| MySQL 并发测试源码 | 代码及离线审查通过；实际执行待门禁 | 普通/预编译 Statement 观察路径修复后定向 12 项通过；完整后端 289 项中 270 项通过、19 项数据库入口跳过，RC/RR 场景未执行 |
| 整体集成复核 | 本地代码问题已关闭 | 旧响应不会覆盖新身份/角色；仍归当前页面的过时保存会结束 saving 并提示返回首页刷新，已离开页面或较新操作不受影响 |

## 3. 本轮基线验证

- 后端 `mvnw.cmd -o test`：113 项，0 失败、0 错误；103 项通过，10 项 MySQL 门禁测试跳过。测试进程清空专用数据库环境变量，未访问真实数据库。
- 小程序 `npm test`：11 个文件、80 项通过；两套 TypeScript 配置检查通过。
- 后端基线已有 Mockito 动态加载 Java agent/CDS 警告；本轮不借机升级依赖或修改全局 JVM 设置，不称为“无警告”。
- 以上仅为修改前基线，不能作为新功能完成证据。

## 4. 最新离线证据与已关闭问题

- 2026-09-06 10:50:15 后端最新完整复跑：`mvnw.cmd -o test`，289 项、0 失败、0 错误、19 跳过，即 270 项通过，退出码 0。所有四个 `DB_TEST_*` 输入显式清空，未执行 clean/package/verify。跳过分布为原身份测试 1、迁移保护 3、迁移 6、新邀请/成员参数化方法入口 9；这不是 19 项数据库通过。结果与 01:16:18 的离线回归一致，既有 Mockito/Java agent/CDS 警告仍单独保留。
- 01:27:38 小程序当时为 156 项通过。用户继续授权修复后，补齐资料页保存状态断言，真实 RED 为 24 项中 6 个断言失败；修复后专项 24 项通过。主代理于 10:43:35 独立完整复跑，16 个文件、160 项通过，随后两套 TypeScript 检查通过。普通保存、旧退出/昵称响应保护、隐藏/卸载和较新保存操作保护仍通过。
- V1/V2 与本轮前快照逐字节一致；8 个页面的四类资源齐全，9 个小程序 JSON 均可解析，`git diff --check` 通过。原 JAR 校验仍等于此前记录的可信摘要，没有重新打包、更新 JAR 或修改凭据；真实 MySQL 自动测试仍未执行。
- 已关闭的最后问题：资料页曾将当前操作归属与响应上下文有效性合为一个 guard，导致过时响应的 catch/finally 都不执行。现在仅以操作归属决定释放 saving，并在上下文过时时给出“可能已生效、返回首页刷新确认”的通用提示；具体旧错误和请求编号不带入新身份，不自动重试，不写回旧昵称或角色，不触发旧导航。独立复审明确批准此 Important 已解决。
- 当前请求如果已经发到服务端，丢弃旧响应不代表服务端回滚。页面提示要求明确刷新确认；测试包括身份切换、同 token 转让、刷新尚未完成、已成功返回但页面续执行前上下文变化，以及用户刷新后再次主动保存。

### 电脑重启后的原应用恢复

用户在新 SSH 窗口完成服务器密码输入；已核对 SSH PID 38996 监听回环 13306，原后端 Java PID 37816（父 PowerShell 47108）监听回环 7631。主代理独立检查健康接口 HTTP 200、`status=UP`。微信开发者工具的原项目窗口已经打开，未重复启动。现有两份加密配置的大小/修改时间保持不变，由原启动脚本复用，未显示敏感内容。

这些 PID 仅是本次恢复快照，不可供未来直接停止进程。恢复的是 2026-09-05 打包的原版本，不包含本次 Task 6 新后端接口；不要据此验收邀请/成员功能。保留两个运行窗口，不重新初始化账本。

## 5. 后续受控验收

本地最终复核问题已关闭。2026-09-06 用户允许真实 MySQL 门禁并完成本机输入，专用测试批次已启动，仅使用固定 `account_book_test`、专用测试账号和现有回环隧道。随后用户明确暂不验证“争夺第 10 个成员名额、拒绝第 11 人”场景；已停止本次测试 JVM，Maven 随后退出，没有重跑或追加清理。原 SSH 和后端仍在，健康接口 HTTP 200、UP。

本轮状态为**中断，未通过验收**：未取得本次完整 XML 报告，不能从线程推进情况推定其他场景已通过，也不能借用旧的跳过报告。测试库可能留有本次临时数据；未清理开发库、未修改成员上限规则、未替换 JAR。容量场景按用户要求暂缓，其余真实验证仍待确认后继续。

本次选择 `InviteMemberMysqlIntegrationTests`，原预期九组 × RC/RR 共 18 项；中断后不记录为通过。旧基础门禁脚本既未选择该新测试，也包含旧 `AuthMysqlIntegrationTests` 超时后未确认工作线程终止即可能清理的问题，因此不复用该完整脚本；此旧用例问题留待后续修复并复核，本次不修改它。单次操作入口已通过只读审查及 Windows PowerShell 5.1 语法验证，但自定义报告目录参数的实际效果尚未验证，下次执行前须检查。失败或中断不自动重试，不额外清理，不打包。

通过真实 MySQL 门禁后，才申请重新打包及替换运行后端进行新功能验收。现在微信开发者工具即使看到新入口，也不能据此认为旧运行 JAR 已支持新接口。

执行前的固定边界：

1. 重新核对回环隧道与目标身份，只允许 `account_book_test_app@127.0.0.1` 访问 `account_book_test`；不从开发库加密配置读取或借用凭据。
2. 用户单独允许清理测试库，并保证没有第二个测试进程或测试库写入客户端。JUnit 资源锁仅协调同一次运行，不是跨进程互斥。
3. 实际验证记录 RC/RR 下的并发结果、完整回滚和旧会话失效；目前编译成功或跳过不能替代这些证据。
4. 数据库门禁通过后，再确认本地运行后端更新。保留原初始化口令、Pepper、AppID 和本机加密凭据，不重新初始化现有账本。

人工验收逐项记录实际身份和环境：

- OWNER 创建邀请、查看状态、撤销；列表不提供恢复原码。
- 未加入成员接受一次邀请；同一码第二次无效，已有效成员不会消费它。
- ADMIN 无法管理 OWNER/其他 ADMIN；OWNER 先降级再移除 ADMIN。
- 非所有者退出账本后旧会话失效；新邀请重新加入为 MEMBER。
- 转让后新 OWNER 生效、旧 OWNER 成为 MEMBER，旧管理者未使用邀请撤销。
- 邀请原文不进入 Storage/日志；退出登录与退出账本操作明确区分。

本机模拟器不能证明跨手机分享成功。真实多人/三角色与跨设备验收留待合规 HTTPS 环境；不通过开放公网 7631/3306 处理真机联调。剩余 Network/Storage/Console 发布检查只反馈是否通过，禁止发送敏感值。
