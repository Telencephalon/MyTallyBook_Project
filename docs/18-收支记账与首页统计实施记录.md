# Task 8/9：收支记账与首页统计实施记录

> 2026-09-11 更新：统计页已扩展为本月、全部、自定义日期范围，默认当前自然月；每日趋势按 31 天分页且不影响全范围汇总/排行。Task 9 代码级独立复核通过。Task 10 分类/账户草稿隔离收尾也已完成复核；运行 JAR、真实 MySQL 统计对照和双设备验收仍未执行。当前状态以[统计范围升级与交互收尾记录](21-统计范围升级与交互收尾记录.md)为准，下面历史条目保留原始证据。

> 2026-09-10 关联进度：Task 10 子任务 5 已在主目录完成隐私/静态路由/配置审计并同步四份文档。小程序当前 `app.json` 注册 20 页、四栏 tab，注册页四件套检查及 tab metadata 对 `TAB_ROUTES` 均通过；新增真实 handler 覆盖 Home 明细/统计、Settings 栈链接、Profile 成功保存返回、EntryDetail 授权删除后切换明细 tab。Focused audit 文档更新前 67/67、补充覆盖后 72/72 通过。配置编辑位置为 `account-book-miniapp/miniprogram/config/env.ts:3` 的 `DEPLOYED_API_ADDRESSES`、`account-book-miniapp/miniprogram/config/app-info.ts:11-18` 的公开版本/运营方/联系方式/备案字段；当前仍待临时公网 HTTPS 域名、ICP/小程序备案、TLS、微信 request 合法域名、运营方和联系方式，配置前 trial/release 不允许请求。该证据只说明代码与离线行为状态，不代表 Task 10 全部、Mate70Pro/iPhone 15 真机、备案/TLS/微信合法域名或 Task 11/12 已完成；Task 9 真实 MySQL/人工 SQL 对照仍按本记录既有状态单独管理。详见 [Task 5 子任务报告](../.superpowers/sdd/2026-09-09-task10-interaction-privacy-device-implementation/task-5-report.md)。

## 2026-09-09 用户运行与操作验收回执

用户明确反馈“task9已启动并验证成功”，因此关闭下方旧启动锁所造成的待用户操作暂停点，记录 Task 9 用户运行与小程序操作验收通过。本轮未代为重启、查询私人业务数据或运行统计专项测试；回执不能扩展成五组统计固定数据与人工 SQL 对照已通过、双真机已验收或生产已部署。后续进入 Task 10 设计梳理，见 `20-交互隐私真机与后续部署准备记录.md`。以下均为当时证据，不覆盖此新回执。

## 2026-09-08 Task 9 主目录开发续跑

用户确认只在 `D:/Work/Workplaces/privateWork/AAProject/MyTallyBook_Project` 继续后续开发，旧隔离目录停止使用；业务沿用已批准的 Task 7–9 规格。本轮只开发首页与统计，不执行 Task 10–12，不清理开发库或测试库，不修改迁移和加密配置。用户随后单独授权：验证通过后更新并重启主目录本地后端，保留 SSH、数据库与全部加密配置。

21:00 基线：后端离线报告后缀 `task9-baseline-20260908`，347 项中 321 通过、26 门禁跳过，0 失败/错误；小程序 219 项及两套 TypeScript 检查通过。Mockito 动态 agent/JVM 提示为继承警告。21:04 只读运行检查：SSH 13306 可连、后端健康 UP，均为已有进程，本轮未启停。

实现及两级独立复核已完成，无剩余 Critical/Important 问题。后端本轮新报告 `task9-full-20260908`：370 项中 344 通过、26 门禁跳过、0 失败/错误。22:18 最终小程序独立复跑：24 文件、244 项通过，两套 TypeScript 检查通过。最终原生 WXML 编译 17 页退出码 0，每页四件套齐全，输出 127123 字节。235/237 项为较早修复轮次的历史证据，不是最终计数。

复核修复了统计页在请求中或完整读取失败后切换收支方向导致数据/重试入口丢失的问题；首页汇总和最近账单各自独立重试，过期响应不能覆盖新请求结果。修复前新增用例明确失败，修复后通过。未通过弱化断言或跳过失败用例完成验证。

原生编译首次诊断命令的输出相对路径写错、目标目录不存在，编译器异常退出；改为验证过的主目录绝对输出路径后，同一份源码编译通过，不涉及源码修复或忽略编译错误。

| 交付层次 | 本轮状态 |
| --- | --- |
| Task 9 源码 | 首页卡片、全日期最近五笔、统计页及五个接口已实现，两级独立复核通过 |
| 离线回归 | 后端 344 通过/26 门禁跳过，小程序 244 通过、两套 TS 与 17 页 WXML 通过 |
| 真实 MySQL 专项 | 未执行；未获得新一轮独占清理授权，不沿用 Task 7/8 已消耗的授权 |
| 运行包更新 | 已获授权并调用一次；在启动互斥锁阶段退出，尚未停止服务、打包或替换 JAR，等待解除旧启动窗口的“选择”状态 |
| 小程序/真机人工验收 | 待新运行包更新后用户操作；未自动标记通过 |

接口均要求现有有效成员身份，采用账本一致快照读取：

| GET 路径（前缀 `/api/v1/statistics`） | 参数与用途 |
| --- | --- |
| `/monthly-summary` | 可选 month，返回 income/expense/net/entryCount |
| `/daily-trend` | 可选 month，补齐整月逐日收支、结余与笔数 |
| `/categories` | 可选 month、entryType，分类金额、占比及笔数 |
| `/accounts` | 可选 month，按账户展示本月收入、支出、净额及笔数 |
| `/members` | 可选 month、entryType，按账单创建人统计金额、占比及笔数 |

month 严格为 `YYYY-MM`，范围 `1000-01` 至 `9999-12`；缺省按 Asia/Shanghai 当前月，最大月份不构造 10000 年上界。分类/成员默认 EXPENSE，可切换 INCOME。金额与百分比返回固定两位小数字符串；前端不累加浮点货币。没有新增依赖、数据库表或迁移。

测试先证明缺少统计模块、首页取数和导航的失败，再实现到定向及完整回归通过。实现报告与任务差异保留在主目录 `.superpowers/sdd/2026-09-06-task7-9-bookkeeping-implementation/`，原有 Task 7/8 代码和验证证据不覆盖。

### 后端更新暂停点（2026-09-08 22:24）

使用主目录 `Start-Dev.ps1 -UpdateBackend` 调用一次，退出码 1，安全分类 `StartupAlreadyInProgress`。互斥锁检查在备份、停止服务和打包之前，故本次没有改变运行服务或 JAR，也没有自动重试。22:23 检查旧后端 7631 为 UP，SSH 13306 的原 PID 19392 与后端原 PID 20540 均保持。

只读进程关系确认：旧主目录 `Start-Dev.ps1 -UpdateBackend`（PID 24540，20:43 启动）仍存在，其父链通向标题为“选择 管理员: Windows PowerShell”的窗口（PID 59720）；SSH 和旧后端启动窗口由该旧启动器开启。控制台选择模式可能暂停旧启动器输出并阻止释放互斥锁。未强杀这些进程、修改锁或绕过安全检查。

下一步请用户在**旧一键启动命令窗口**按 `Esc` 退出选择模式；不要关闭 SSH 或后端窗口，不要重复输入密码或启动命令。用户确认后先只读检查旧启动器是否退出，再继续已授权的主目录更新。当前代码可审阅，但尚不能把旧运行包当成 Task 9 预览版本。

### 统计口径与后续人工验收

- 首页本月收入/支出/结余只统计当月未删除账单；账户初始余额不属于收入和月结余。
- 最近五笔按业务日期倒序、ID 倒序，覆盖所有日期，不只取本月；允许未来业务日期。
- 统计页支持月份切换、日趋势、分类/账户/成员统计；分类和成员可切换收入/支出。停用字典及已退出创建人的历史账单仍参与。
- 每日趋势补齐整月，空数据与读取失败分开显示；失败不能显示虚假的全零统计。
- 上线到本地运行包后，从首页“统计”入口检查；切换月份及收入/支出，核对与账单列表相同月份、相同方向的一致性。
- 样例人工验算：同月收入 100.10 与 0.20、支出 30.05，应为收入 100.30、支出 30.05、结余 70.25、3 笔；软删除的 9.99 和其他月份的 8.00 不计入。仅使用明确标记的验收数据，不删除真实原有账单来造样本。

以下带日期内容为历史证据，不覆盖本节的当前续跑状态。

> 后续状态（2026-09-08）：Task 8 的真实验收通过后，代码已归并主目录，后端运行路径已切换并健康 UP；用户尚需在微信开发者工具手动导入主目录项目。Task 9 未开始。详见 [目录切换记录](19-主目录归并与运行目录切换记录.md)。下方时间戳及“未归并”描述为当时的历史记录。

日期：2026-09-06；更新：2026-09-08 16:13。状态：Task 8 本地实现、离线验证、后端更新及用户小程序操作验收完成；修正测试请求二次编码后，本轮七组真实 MySQL 专项全部通过，0 失败/错误/跳过，启动器退出码 0。Task 9 与主目录归并未开始。

## 2026-09-08 本轮真实复验通过

用户明确授权“允许新一轮独占清理 account_book_test，复验这 7 组用例”。16:06:44 打开唯一输入窗口 `MyTallyBook Task78 Retest - 20260908`（PID 11980），用户完成本机确认及隐藏密码输入；16:11:50 执行完成。包装脚本只调用原门禁一次，记录 `launcherExitCode=0`；未自动重试。

本轮报告：`account-book-server/target/surefire-reports/TEST-com.mytallybook.accountbook.database.BookkeepingMysqlIntegrationTests-c438cc1a8dde4d8398ce494f1495ee52.xml`，生成于 16:11:50。完整 XML 校验及逐项方法名核对均通过，恰好为批准的七组，每项均无 failure/error/skipped 节点：

| 真实验证范围 | 结果 |
| --- | --- |
| 分类/账户排序规则唯一性、不可变字段及版本控制 | 通过 |
| 账单幂等重放、编辑/软删除、余额与历史引用保护 | 通过 |
| 停用关联保留、特殊字符字面关键词与最大日期组合筛选 | 通过；原失败项关闭 |
| 历史创建人保留、当前权限及跨用户操作限制 | 通过 |
| 成功记账的审计记录不落库私人备注 | 通过 |
| 业务 DML 与审计写入后的物理事务回滚 | 通过 |
| 编辑/软删除的审计失败回滚 | 通过 |

执行前后运行 JAR 与 V1/V2 的 SHA-256 均匹配既有交付值。测试进程已退出，原 SSH 进程 44560 保留；业务后端在本轮开始前就未运行，本轮未启动或重启它。仅按授权使用测试库，没有开发库/生产库清理、业务代码或密码/密钥变更，没有提交、归并或 Task 9 开发。

本轮有 Surefire 关于 Windows Manifest-JAR 绝对 classpath/不同根路径的提示，保留在 `2026-09-08T16-07-12_222.dumpstream`；未造成测试失败，不为该提示扩展本次修正。原失败报告与本轮成功报告分别保留；离线后端 321 通过/26 跳过是上一轮独立证据，不与本次 7/7 合并为一次测试总数。

后续路线图前置真实验收已满足；主目录归并、运行目录切换和 Task 9 尚未执行。本轮结果不扩大为容量竞争或完整 Android/iOS 验收。

## 2026-09-08 真实结果核对与请求编码修复

本节覆盖下方“尚待执行”的历史状态。11:46:56 打开的修正版窗口已完成真实执行，11:51:49 生成的完整报告 `TEST-com.mytallybook.accountbook.database.BookkeepingMysqlIntegrationTests-cf9eeca8a7a040ebb6e81a104bcd7eaf.xml` 确认：7 项，6 通过、1 失败、0 错误、0 跳过。该报告保留在 `account-book-server/target/surefire-reports/`，本轮离线验证使用不同报告后缀，不覆盖原报告。

- 通过：分类/账户约束、账单防重与余额/引用保护、历史创建人与当前权限、审计备注隐私、业务写入后物理事务回滚、编辑/软删除审计失败回滚。
- 失败：`disabledUnchangedAssociationsAndLiteralKeywordRemainUsable` 第 197 行。停用关联编辑与新建拒绝已执行到预期状态；随后特殊字符与最大日期组合筛选预期 1 条，实际 0 条。
- 离线根因：MockMvc 的 String URL 模板重编码了已编码参数 `%25_%5C`，使请求参数仍为 `%25_%5C`，而非目标字面量 `%_\`。独立请求适配测试不启动 Spring 上下文、不连接数据库，已直接复现这个边界。
- 修正：真实测试共用的 `gateRequest` 改用 `URI` 重载，保持原始编码只解码一次；保留七组真实用例、SQL 业务代码、断言和测试库门禁。新增三项离线回归，覆盖 `%`/`_`/反斜杠、中文/查询分隔符/字面百分号以及删除版本参数。
- RED：请求回归 3 项中 2 失败、1 通过，明确显示参数二次编码。GREEN：请求回归及账单 HTTP、过滤器、JDBC 查询定向验证 14 通过、7 项真实库门禁跳过，0 失败/错误。15:59:34 完整后端离线回归退出 0；46 份本轮 XML 报告合计 347 项，321 通过、26 门禁跳过、0 失败/错误。
- 本轮未重新运行真实测试、未开启新的测试库清理窗口；未修改业务源码、小程序、运行 JAR、V1/V2 或凭据，未重启、提交或归并。不能以离线 GREEN 宣称原真实失败项已经通过。

验证命令均在 `account-book-server` 执行，先清除当前测试子进程的数据库、微信、应用和 JVM/Maven 注入变量，设置既有 Java 21；未读取或输出凭据。新报告使用独立后缀：

```powershell
.\mvnw.cmd -o '-Dtest=BookkeepingMysqlRequestTests' '-Dsurefire.reportNameSuffix=task78-request-red-20260908' test
.\mvnw.cmd -o '-Dtest=BookkeepingMysqlRequestTests,EntryHttpTests,EntryFiltersTests,JdbcEntryStoreTests,BookkeepingMysqlIntegrationTests' '-Dsurefire.reportNameSuffix=task78-request-green-20260908' test
.\mvnw.cmd -o '-Dsurefire.reportNameSuffix=task78-request-full-20260908' test
```

首条为修复前的失败证据，修复后无需人为恢复缺陷重跑 RED。自复核确认只调整测试请求适配，未弱化原失败断言，未改变七组门禁选择和清库机制；本轮未调用独立审查代理。当前运行 JAR 和 V1/V2 的 SHA-256 与原交付记录一致，`git diff --check` 通过。继承的 Mockito 动态 agent/JVM 提示仍存在。小程序无本轮改动，219/219 与类型/WXML 结果仍引用上次证据。

此处修复后的下一步已完成：本轮独占 `account_book_test` 七组真实复验全部通过，见顶部最终结果。主目录归并与 Task 9 仍为后续事项。

2026-09-08 前置收尾：Task 7/8 真实 MySQL 测试入口的离线准备与独立复核已完成。用户现已确认“Task 8 小程序操作已验收通过，允许本次独占清理 account_book_test 并执行真实验证”。本次只运行已复核的七组 Task 7/8 用例，不自动重试；截至授权记录时尚未清理测试库、归并主目录或开始 Task 9。下文标注 2026-09-06 的数字是上次功能交付证据，不代表本次重新执行。

## 2026-09-08 测试入口准备结果

- 新增一个真实 MySQL 集成测试类，包含七组 Task 7/8 用例：分类/账户约束、账单防重与余额、停用关联及历史创建人、审计隐私和物理事务回滚。Task 9 的统计断言继续保留在未启用草稿中。
- Windows PowerShell 5.1 脚本离线检查 22/22 通过，主代理独立重跑通过；覆盖成功、取消、预检失败、子进程异常、密码变量清理及完整 XML 结果校验。
- 后端完整离线回归：344 项，318 通过、26 按条件跳过、0 失败/错误。修正一处测试预期状态码后，定向重新编译成功，七组真实数据库用例全部按预期跳过，不能记为业务验证通过。
- 独立复核提出的四项问题已修正并复核通过：Maven 日志/退出码混合、停用关联状态码、截断 XML 误判、启动流程测试不足。
- 运行 JAR、加密配置、V1/V2、开发库均未改动；没有重启服务、提交或归并。主目录的已有 application.yml 改动保留。
- 真实执行入口位于 `.superpowers/sdd/2026-09-06-task7-9-bookkeeping-implementation/run-bookkeeping-mysql-gate.ps1`。仅在本次测试库独占清理授权确认后运行；需要现有 SSH 通道、输入 `TEST` 和 `account_book_test_app` 密码，不使用开发库密码。通过条件为本次新报告恰好七项通过、零跳过、零失败/错误；失败不会自动重试。

当时下一步：复核现有 SSH 通道，打开独占测试密码窗口，执行已授权的真实验证并核对新报告。当前结果与后续步骤以本文顶部更新为准。

### 本地预检中断（2026-09-08）

首次窗口在输入 `TEST` 后、本地 Java 版本检查时退出，尚未提示数据库密码，未开始真实测试或清理测试库。已在 Windows PowerShell 5.1 复现：`java -version` 的正常 stderr 输出被 `ErrorActionPreference=Stop` 与 `2>&1` 组合转成异常；不是密码错误。

已修正为独立读取原生进程的 stdout/stderr，保留退出码与 Java 21 校验。新增实际 Java 调用回归及失败边界检查；本地脚本 24/24 通过，主代理独立重跑通过，专项代码复核通过。真实七组用例仍未执行；修正后重新打开人工确认窗口，必须再次输入 `TEST` 和测试库密码，不能把本地脚本通过当成数据库业务通过。

## 授权与范围

用户确认 Task 7 测试正常，并明确要求恢复 Task 8、Task 9。沿用已批准的 [规格](superpowers/specs/2026-09-06-task7-9-bookkeeping-design.md) 与 [计划](superpowers/plans/2026-09-06-task7-9-bookkeeping-implementation.md)，按两项纵向功能逐一实施与复核。

最新调整：完成 Task 8 后更新并重启后端供用户验收，Task 9 等待用户再次确认。此次重启授权不包含测试库清理、数据库迁移、密码或密钥修改。

- Task 8：新增收入/支出、账单列表/详情/筛选、修改、软删除、UUID 防重复提交、版本冲突、最新成员权限。
- Task 9：当月汇总、全日期最近五笔、每日趋势、分类/账户/创建人成员统计。
- 金额传输与展示使用两位十进制字符串；月净额不包含账户初始余额。
- 保留 Task 7 字典、资料、邀请、成员和退出登录功能；不增加转账、预算、导出或完整 Task 10 导航。
- 仅修改 `codex-mvp-development` worktree，不提交、不推送，不改 V1/V2、数据库密码、AppID/AppSecret、APP_BOOTSTRAP_KEY 或 Pepper。

## 开始时验证

2026-09-06 20:39，后端离线测试退出 0：40 个新报告合计 310 项，291 通过、19 项数据库门禁跳过、0 失败/错误；小程序 180/180（18 个文件），两套 TypeScript 配置通过。Mockito 动态 agent/JVM 提示为既有警告。

## 当前交付层次

| 层次 | 状态 |
| --- | --- |
| Task 8 代码与离线回归 | 更新打包的 44 份新报告：337 项中 318 通过、19 跳过、0 失败/错误；小程序独立回归 219/219、两套 TS 与 16 页 WXML 通过 |
| Task 8 独立复核 | 任务级、最终 Task 7/8 集成及修正复核通过，无阻塞问题 |
| Task 9 代码、回归与复核 | 用户要求暂停；尚未开始 |
| 真实 MySQL 专项 | 2026-09-08 16:11:50 本轮七组全部通过，0 失败/错误/跳过，退出码 0；原请求编码失败项关闭 |
| Task 8 运行 JAR 更新 | 22:28 更新完成，HTTP 健康检查 UP，原 SSH 复用 |
| Task 8 小程序操作验收 | 用户于 2026-09-08 确认通过；不扩大为完整 Android/iOS 真机验收 |
| Task 9 与完整真机验收 | 未执行 |

完整阶段记录、测试证据、审查快照及验收清单保留在 `.superpowers/sdd/2026-09-06-task7-9-bookkeeping-implementation/`。既有未提交工作不清理，也不以健康 UP 或离线测试替代真实业务验收。

## 本次更新证据

- 受控更新退出码 0；新运行 JAR 包含记账、分类、账户控制器，Task 9 统计类不存在。新包 SHA-256：`0fb31a76621b0ebc7cbe578da6e334846b4509ee5acffb304cf84909eb4c4a95`。
- 旧 Task 7 包保存在 `backup/local-backend/account-book-server-20260906-142631-5564172-e52efb9e9cc1.jar`，备份哈希与更新前一致，可供人工恢复；未自动回滚。
- 22:28:42 启动日志确认开发库版本仍为 2、无需迁移；22:28:46 应用在 `127.0.0.1:7631` 启动，stderr 为空。SSH `127.0.0.1:13306` 保留。
- 独立复查确认新项目 Java 进程与健康 UP；未登录访问记账接口返回 401。这仅证明启动与鉴权边界，不代表登录后的新增/修改/删除已经完成真实业务验收。
- `keys.dpapi`、`credentials.dpapi` 和 V1/V2 均未改动；没有修改数据库密码、AppSecret、初始化密钥或 Pepper，没有清理开发库/测试库。
- 进度保存并停止开发，等待用户验收 Task 8 后再确认 Task 9。

## 更新完成后的验收入口

1. 在微信开发者工具点击“编译”，进入首页“收支记账”。点击“记一笔”新增收入/支出，或“查看账单”进入列表。
2. 选择分类、账户、日期并填写金额；保存后进入账单详情，可根据权限修改或删除。列表支持日期、类型、分类、账户、创建人和备注筛选。
3. 用明确标注的验收数据检查账户余额随新增、修改、软删除变化；不要删除原有真实账单作为测试。
4. 遇到“提交结果未知”时，只使用“使用原提交标识重试”，不要退出小程序后重新录入同一笔。UUID 与草稿只在内存保留；受支持的返回操作会提醒，但微信侧滑返回、退出或进程终止无法完全拦截。
5. 遇到编辑冲突时保留输入，由用户确认是否重新加载；普通成员不得修改他人的账单。

此处是待执行清单，不表示人工或真实数据库业务验收已经通过。首页统计、每日趋势和分类/账户/成员统计属于暂停中的 Task 9，不在本次预览范围。

非阻塞跟进：在账单列表后续页打开期间，若其他成员恰好删除该页最后一行，空结果面板暂不显示“上一页”；点击“应用”或“清除”可回到第 1 页。此次未为该小项扩展已关闭的修正范围。
