<view class="page">
  <view class="intro"><text class="eyebrow">共享账本 · 成员邀请</text><text class="title">加入共享账本</text><text class="description">仅在你确认加入时获取微信身份。每个邀请仅可使用一次，请向所有者或管理员获取。</text></view>
  <view class="card">
    <text class="label">邀请码</text>
    <textarea class="token-input" value="{{inviteToken}}" placeholder="请手动粘贴完整的 43 位邀请码" maxlength="256" disabled="{{busy}}" bindinput="onTokenInput" />
    <text class="hint">注意大小写；不会自动读取剪贴板。</text>
    <button class="button button-primary" loading="{{busy}}" disabled="{{busy}}" bindtap="onAccept">确认加入</button>
  </view>
  <view class="error-panel" wx:if="{{errorMessage}}"><text class="error-message">{{alreadyMember ? '你已是账本成员，无需再次使用邀请。' : errorMessage}}</text><text class="request-id" wx:if="{{requestId}}">请求编号：{{requestId}}</text></view>
  <button class="button button-secondary" wx:if="{{alreadyMember}}" disabled="{{busy}}" bindtap="openEntry">前往登录 / 首页</button>
  <text class="description footnote">如果上次加入后网络中断，可通过普通微信登录恢复，无需再次使用邀请。</text>
  <button class="button button-secondary" wx:if="{{!alreadyMember}}" disabled="{{busy}}" bindtap="openEntry">返回登录 / 首页</button>
</view>
